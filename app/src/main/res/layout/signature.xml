<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_wrapper"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="2dp"
    android:paddingBottom="4dp"
    android:paddingLeft="6dp"
    android:paddingRight="6dp"    
    android:background="@drawable/bg32">
    

 <RelativeLayout
     android:id="@+id/rl_signature"
     android:layout_width="match_parent"
     android:layout_height="wrap_content"
     android:background="@null"
     android:padding="4dp" >   
  
     <LinearLayout            
         android:id="@+id/canvas"
         android:layout_width="600dp"
         android:layout_height="300dp"         
         android:layout_centerHorizontal="true"
         android:orientation="vertical"
         android:background="#FFFFFF">
     </LinearLayout>

     <LinearLayout        
         android:id="@+id/sig_buttons"   
         android:layout_width="match_parent"
         android:layout_height="wrap_content"
         android:layout_below="@+id/canvas"
         android:layout_alignEnd="@+id/canvas"
         android:layout_marginTop="6dp"
         android:orientation="horizontal" 
         android:paddingTop="6dp" >

         <Button
             android:id="@+id/sig_clear"
             android:layout_width="wrap_content"
             android:layout_height="wrap_content"
             android:layout_weight=".5"
             android:layout_marginEnd="12dp"
             android:contentDescription="@string/btn_clear"
             android:background="@drawable/custom_button_click"  
             android:text="@string/clear"
             android:textColor="#fff" />

         <Button
             android:id="@+id/sig_save"
             android:layout_width="wrap_content"
             android:layout_height="wrap_content"
             android:layout_weight=".5"
             android:layout_marginStart="12dp"
             android:contentDescription="@string/btn_savesignature" 
             android:background="@drawable/custom_button_click"  
             android:text="@string/btn_savesignature"
             android:textColor="#fff"/>
     </LinearLayout>
 </RelativeLayout>
 </LinearLayout>  
